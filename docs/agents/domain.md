# Domain docs

Layout: **single-context**

- `CONTEXT.md` at repo root (one per project)
- ADRs in `docs/adr/`

Consumer skills (`improve-codebase-architecture`, `diagnose`, `tdd`) read
`CONTEXT.md` for domain language and `docs/adr/` for past architectural
decisions.